This is a minimal example site showing how to use the docusaurus-plugin-terminology plugin.

Start the site (from this folder) with the typical Docusaurus commands after installing dependencies:

1. npm install
2. npm start

The plugin is configured to use `docs/terms` and to hide individual term entries from the sidebar.
